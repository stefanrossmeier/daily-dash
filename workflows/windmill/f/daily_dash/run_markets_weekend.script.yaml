summary: Run DailyDash Weekend Markets
description: Retrieve public IG weekend quotes and write one immutable snapshot artifact. Delivery is orchestrated separately after durable persistence.
lock: ''
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    data_repo:
      type: string
      description: Local DailyDash data repository path used as the output sink.
      default: /data/daily-dash-data
      originalType: string
  required: []
