DailyDash X Watchlist is a curated market/macro source set. Search all allowed handles, not only the most active account. Retrieval completeness across the source set matters more than deciding importance here.

The application will enforce the exact scheduled timestamp window after retrieval, so use the supplied date range as the broad search envelope and return matching source evidence within that envelope.
